import createContainer, {type ContainerProps} from '../utils/createContainer'

export type CollapsedProps = ContainerProps

const Collapsed = createContainer()

Collapsed.displayName = 'DragExpandView.Collapsed'

export default Collapsed
