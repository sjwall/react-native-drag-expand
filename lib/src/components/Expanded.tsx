import createContainer, {type ContainerProps} from '../utils/createContainer'

export type ExpandedProps = ContainerProps

const Expanded = createContainer()

Expanded.displayName = 'DragExpandView.Expanded'

export default Expanded
